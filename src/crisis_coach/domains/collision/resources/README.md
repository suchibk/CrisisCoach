# Collision resources

`evidence_catalogue.json` version 1 contains validated instructions, evidence values, and scenario applicability. It includes no legal-policy assertions. Other resource types and source corpora remain planned.
