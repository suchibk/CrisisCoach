# Scenario evaluation

The scenario runner and metrics will be implemented after the shared engine. Replays must use a scripted clock and fake providers. Safety violations and prohibited coaching fail evaluation. Unit and integration tests remain under tests/.
